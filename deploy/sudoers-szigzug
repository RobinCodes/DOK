# Lets the szigzug user restart its own service during a deploy, and nothing else.
# Install with: sudo install -m 440 deploy/sudoers-szigzug /etc/sudoers.d/szigzug && sudo visudo -c
szigzug ALL=(root) NOPASSWD: /usr/bin/systemctl restart szigzug, /usr/bin/systemctl is-active szigzug
